<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="vyaparsathi.Views.BackupRestorePage"
    Title="Backup &amp; Restore"

    BackgroundColor="#F8FAFC">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="24">

            <!-- Header -->
            <VerticalStackLayout Spacing="6">
                <Label Text="Backup &amp; Restore"
       FontSize="26"
       FontAttributes="Bold"
       TextColor="#0F172A"/>


                <Label Text="Securely backup or restore your business data"
                       FontSize="14"
                       TextColor="#64748B"/>
            </VerticalStackLayout>

            <!-- Backup Card -->
            <Frame CornerRadius="18"
                   BackgroundColor="White"
                   Padding="18"
                   HasShadow="True">

                <VerticalStackLayout Spacing="16">

                    <Label Text="Backup Data"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#1E293B"/>

                    <Label Text="Select the data you want to include in backup"
                           FontSize="13"
                           TextColor="#64748B"/>

                    <!-- Backup Options -->
                    <Grid RowSpacing="12" ColumnSpacing="12">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <HorizontalStackLayout Grid.Row="0" Spacing="10">
                            <CheckBox x:Name="CategoryCheck"/>
                            <Label Text="Categories" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="1" Spacing="10">
                            <CheckBox x:Name="ItemCheck"/>
                            <Label Text="Items" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="2" Spacing="10">
                            <CheckBox x:Name="UdharCheck"/>
                            <Label Text="Udhar Records" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="3" Spacing="10">
                            <CheckBox x:Name="BillingCheck"/>
                            <Label Text="Billing History" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Row="4" Spacing="10">
                            <CheckBox x:Name="ProfileCheck"/>
                            <Label Text="Business Profile" VerticalTextAlignment="Center"/>
                        </HorizontalStackLayout>
                    </Grid>

                    <!-- Backup Button -->
                    <Button Text="Backup Now"
                            HeightRequest="52"
                            CornerRadius="16"
                            FontAttributes="Bold"
                            BackgroundColor="#2563EB"
                            TextColor="White"
                            Clicked="OnBackupClicked"/>
                </VerticalStackLayout>
            </Frame>

            <!-- Restore Card -->
            <Frame CornerRadius="18"
                   BackgroundColor="White"
                   Padding="18"
                   HasShadow="True">

                <VerticalStackLayout Spacing="16">

                    <Label Text="Restore Backup"
                           FontSize="18"
                           FontAttributes="Bold"
                           TextColor="#1E293B"/>

                    <Label Text="Restore data from a previously saved backup file"
                           FontSize="13"
                           TextColor="#64748B"/>

                    <Button Text="Select Backup File"
                            HeightRequest="48"
                            CornerRadius="14"
                            FontAttributes="Bold"
                            BackgroundColor="#F59E0B"
                            TextColor="White"
                            Clicked="OnSelectBackupClicked"/>

                    <Button Text="Restore Data"
                            HeightRequest="52"
                            CornerRadius="16"
                            FontAttributes="Bold"
                            BackgroundColor="#16A34A"
                            TextColor="White"
                            Clicked="OnRestoreClicked"/>
                </VerticalStackLayout>
            </Frame>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
